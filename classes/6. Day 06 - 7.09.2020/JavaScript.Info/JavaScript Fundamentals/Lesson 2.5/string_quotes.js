/*

  String Quotes
  
  Instructions:
  
  What is the output of the script?

  let name = "Ilya";
  alert( `hello ${1}` ); // ?
  alert( `hello ${"name"}` ); // ?
  alert( `hello ${name}` ); // ?
  
  Responses:
  
  => Alert function outputs the string: hello 1.
  => Alert function outputs the string: hello "Ilya". Got this one wrong. Ran the function in the console and got this instead: hello name.
  => Alert function outputs the string: hello Ilya.

*/
